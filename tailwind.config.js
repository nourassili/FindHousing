/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{html,ts}"],  
  // This pattern matches all .html and .ts files within the src directory and all its subdirectories.
  // we selected which files we want to style
  theme: {
    extend: {},
  },
  plugins: [],
}

